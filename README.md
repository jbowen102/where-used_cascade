# where-used_cascade


## Local package setup (recommended)

From your active Python environment, run:

```bash
pip install -e /path/to/.../where-used_cascade
```

Import using the package namespace to avoid collisions:

```python
from where_used_cascade.some_module import some_symbol
```
